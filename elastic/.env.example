# Copy to elastic/.env (gitignored) and replace every value.
STACK_VERSION=9.5.4
ELASTIC_PASSWORD=change-me
KIBANA_PASSWORD=change-me-too
# 32+ random characters, e.g.: python -c "import secrets;print(secrets.token_hex(32))"
KIBANA_ENCRYPTION_KEY=replace-with-64-hex-chars
ES_HEAP=2g
